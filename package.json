{
  "name": "voxelizer",
  "version": "0.6.0",
  "description": "Professional 2D to 3D voxel converter for pixel art",
  "scripts": {
    "dev": "python3 -m http.server 8080",
    "test": "node --test tests/voxelizer.test.js",
    "test:spec": "node --test --test-reporter=spec tests/voxelizer.test.js",
    "check": "for file in voxelizer/*.js; do node --check \"$file\" || exit 1; done"
  },
  "keywords": ["voxel", "pixel-art", "3d", "sprite", "converter"],
  "author": "Nico Rueda",
  "license": "MPL-2.0",
  "engines": {
    "node": ">=18.0.0"
  }
}
